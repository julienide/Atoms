#ifndef UTILS_H_
#define UTILS_H

// #include <string>
  
  // trim from start
std::string ltrim(std::string s);

// trim from end
std::string rtrim(std::string s);

// trim from both ends
std::string trim(std::string s);

#endif // UTILS_H
